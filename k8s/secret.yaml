# Template only — replace base64 placeholder values before applying.
# In production, manage via sealed-secrets, external-secrets-operator, or Vault.
# echo -n 'your-value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: ghost-architect-secret
type: Opaque
data:
  DATABASE_URL: <your-base64-database-url>
  JWT_SECRET: <your-base64-jwt-secret>
  JWT_REFRESH_SECRET: <your-base64-refresh-secret>
  ADMIN_USERNAME: YWRtaW4=  # admin (base64 encoded)
  ADMIN_PASSWORD: R2hzdEFJyYXJjaGVjdDIwMjAyNQ==  # GhostArchitect2026! (base64 encoded)
